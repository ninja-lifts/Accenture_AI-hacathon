<!-- GENERATED FILE - do not hand-edit. Written by `make fetch-rs && eval --benchmark rs`.
     Committed on purpose, misses included. If you are reading this line in the
     submitted repo, the harness never ran. -->

# RS benchmark — NOT YET GENERATED

`make fetch-rs` then `python -m eval.harness --benchmark rs`.

135 real anomaly cases from a video-delivery system, operator-assigned causes in
`anomaly.yaml` ({data, timestamp, cause}). Seven published algorithms ship with
the RiskLoc repo and run from one command.

```
| Method | F1 | Exact match |
|--------|----|-------------|
| Adtributor | – | – |
| R-Adtributor | – | – |
| Squeeze | – | – |
| RiskLoc | – | – |
| HotSpot | – | – |
| ... | – | – |
| **GlassBox (Stage 03)** | – | – |
```

**Stage 03 only.** None of these baselines does retrieval, falsification or
abstention — there is nothing to compare on those. Keep this table separate from
the trust-behaviour table; merging them puts your least novel component on stage.
