# Assets

Static images referenced by the README, the deck and the docs.

- `kpi_graph.png` — rendered by `engine/kpi_graph.render_png()`
- `screenshot_findings.png` — the findings view, tiers visible, evidence open
- `sc08_comparison.png` — B3's invented cause beside our abstention.
  **This is the single most valuable image in the submission.**
